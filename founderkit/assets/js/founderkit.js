(function(){'use strict';document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('[data-fk-block="testimonial-carousel"]').forEach(function(block){const slides=block.querySelector('.fk-testimonials__slides');const dots=block.querySelector('.fk-carousel-dots');if(!slides)return;const items=slides.children;let current=0;for(let i=0;i<items.length;i++){const d=document.createElement('button');d.className='fk-dot'+(i===0?' is-active':'');d.addEventListener('click',()=>goTo(i));dots&&dots.appendChild(d)}function goTo(i){current=(i+items.length)%items.length;slides.style.transform='translateX(-'+(current*100)+'%)';block.querySelectorAll('.fk-dot').forEach((d,idx)=>d.classList.toggle('is-active',idx===current))}block.querySelector('.fk-carousel-prev')?.addEventListener('click',()=>goTo(current-1));block.querySelector('.fk-carousel-next')?.addEventListener('click',()=>goTo(current+1));});});})();
